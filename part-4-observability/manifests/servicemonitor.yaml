apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: titanic-api
  namespace: titanic-api
  labels:
    app: titanic-api
    release: kube-prometheus-stack
spec:
  selector:
    matchLabels:
      app: titanic-api
      component: api
  endpoints:
  - port: http
    path: /metrics
    interval: 30s
    scrapeTimeout: 10s
    scheme: http
