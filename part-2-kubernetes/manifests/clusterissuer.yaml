apiVersion: cert-manager.io/v1
kind: ClusterIssuer
metadata:
  name: letsencrypt-prod
  labels:
    app: titanic-api
spec:
  acme:
    server: https://acme-v02.api.letsencrypt.org/directory
    email: admin@iyere.site
    privateKeySecretRef:
      name: letsencrypt-prod
    solvers:
    - http01:
        ingress:
          class: nginx
